<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.DotsBoxes" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:colorBackground">@color/finalDarkBg</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:fontFamily">@font/abel</item>
    </style>
    <style name="Widget.App.ButtonStyle"
        parent="Widget.MaterialComponents.Button">
        <item name="materialThemeOverlay">@style/ButtonThemeOverlay</item>
    </style>
    <style name="ButtonThemeOverlay">
        <item name="colorPrimary">@color/btnCustom</item>
    </style>
    <style name="AlertDialogStyle" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:background">@color/finalDarkBg</item>
        <item name="android:textColorPrimary">@color/pinkPlayer</item>
<!--        <item name="android:textAlignment">center</item>-->
    </style>
</resources>
